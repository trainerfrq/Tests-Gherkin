Scenario: Op1 closes open popup window
Then HMI OP1 closes popup notification if window is visible
Then HMI OP1 closes popup maintenance if window is visible

Scenario: Op2 closes open popup window
Then HMI OP2 closes popup notification if window is visible
Then HMI OP2 closes popup maintenance if window is visible

Scenario: Op3 closes open popup window
Then HMI OP3 closes popup notification if window is visible
Then HMI OP3 closes popup maintenance if window is visible
